<Window x:Class="QuizGameJosef2025.CreateQuizWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Skapa quiz" Height="500" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Quiz-namn"/>
            <TextBox x:Name="QuizNameTextBox" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Fråga"/>
            <TextBox x:Name="QuestionTextBox" Height="60" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Svarsalternativ"/>
            <TextBox x:Name="Answer1TextBox" Margin="0,2,0,2"/>
            <TextBox x:Name="Answer2TextBox" Margin="0,2,0,2"/>
            <TextBox x:Name="Answer3TextBox" Margin="0,2,0,2"/>

            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                <TextBlock Text="Rätt svar:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="CorrectIndexComboBox" Width="120">
                    <ComboBoxItem Content="Svar 1"/>
                    <ComboBoxItem Content="Svar 2"/>
                    <ComboBoxItem Content="Svar 3"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <ListBox x:Name="QuestionsListBox"
                 Grid.Row="3"
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Content="Lägg till fråga"
                    Width="120"
                    Margin="0,0,10,0"
                    Click="AddQuestion_Click"/>
            <Button Content="Spara quiz"
                    Width="120"
                    Click="SaveQuiz_Click"/>
        </StackPanel>
    </Grid>
</Window>
